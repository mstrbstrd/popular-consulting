name: Export Metabloom source

on:
  push:
    branches:
      - codex/metabloom-emote-protocol

permissions:
  contents: read

jobs:
  export-source:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out protocol branch
        uses: actions/checkout@v4
        with:
          ref: codex/metabloom-emote-protocol

      - name: Upload source worktree
        uses: actions/upload-artifact@v4
        with:
          name: metabloom-pr117-source-${{ github.sha }}
          include-hidden-files: true
          retention-days: 1
          path: |
            .
            !.git/**
            !node_modules/**
            !build/**
            !coverage/**
