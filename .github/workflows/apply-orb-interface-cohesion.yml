name: Apply Orb interface cohesion

on:
  push:
    branches:
      - feature/orb-interface-cohesion-20260903

permissions:
  contents: write

concurrency:
  group: apply-orb-interface-cohesion
  cancel-in-progress: false

jobs:
  apply-test-and-push:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - name: Check out feature branch
        uses: actions/checkout@v4
        with:
          ref: feature/orb-interface-cohesion-20260903
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Apply guarded interface patch
        run: python scripts/apply_orb_interface_cohesion.py

      - name: Check whitespace
        run: git diff --check

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      - name: Lint
        run: npm run lint

      - name: Run complete test suite
        run: npm test -- --watchAll=false --runInBand
        env:
          CI: true

      - name: Build production bundle
        run: npm run build
        env:
          CI: true

      - name: Commit verified source
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm scripts/apply_orb_interface_cohesion.py
          git rm .github/workflows/apply-orb-interface-cohesion.yml
          git add -A
          git diff --cached --check
          git commit -m "Refine the Metabloom chat interface"
          git push origin "HEAD:${GITHUB_REF_NAME}"
