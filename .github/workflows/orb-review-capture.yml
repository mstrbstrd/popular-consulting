name: Orb visual review capture

on:
  push:
    branches:
      - feature/metabloom-amorphous-avatar-20260902

permissions:
  contents: read

jobs:
  capture:
    runs-on: windows-latest
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      - name: Build production bundle
        run: npm run build
        env:
          CI: true

      - name: Capture faceless Metabloom avatar
        run: node scripts/capture-orb-review.mjs

      - name: Upload Orb review images
        uses: actions/upload-artifact@v4
        with:
          name: orb-review-${{ github.sha }}
          path: orb-review/*.png
          if-no-files-found: error
          retention-days: 3
