name: Polish Morphogen sand paint

on:
  push:
    branches:
      - agent/morphogen-sand-paint
    paths:
      - .github/workflows/polish-morphogen-sand-paint.yml

permissions:
  contents: write

jobs:
  polish-and-validate:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Check out feature branch
        uses: actions/checkout@v4
        with:
          ref: agent/morphogen-sand-paint
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Apply Morphogen paint polish
        run: |
          base64 --decode scripts/polish_morphogen_sand_paint.py.gz.b64 \
            | gzip --decompress \
            > /tmp/polish_morphogen_sand_paint.py
          python /tmp/polish_morphogen_sand_paint.py

      - name: Verify patch whitespace
        run: git diff --check

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      - name: Lint
        run: npm run lint

      - name: Run tests
        run: npm test -- --watchAll=false --runInBand
        env:
          CI: true

      - name: Build production bundle
        run: npm run build
        env:
          CI: true

      - name: Commit and push the polish
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            src/components/CreatorOSFieldCanvas.js \
            src/components/CreatorOSFieldShader.js \
            src/components/DitherCanvasPage.css \
            src/components/MorphogenPaintMode.test.js
          git rm -f \
            .github/workflows/polish-morphogen-sand-paint.yml \
            scripts/polish_morphogen_sand_paint.py.gz.b64
          git commit -m "Polish Morphogen sand paint strokes"
          git push origin HEAD:agent/morphogen-sand-paint
